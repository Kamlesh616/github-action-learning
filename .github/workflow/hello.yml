name: Hello

on:
  push:
    brances: main

jobs:
  greet:
    run-on: ubuntu-latest
    steps:
      - name: say hello to users
      - run: echo "hello dosto"

      - name: say hello to username
      - run: echo "hello $USER"
